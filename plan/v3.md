# 计划

本文档只用于跟踪 `v2` 方案的实际实现进度。它不是设计文档，而是断点续接用的 checklist + commit 对照表。

## plan_v3

状态：实现中

### 1. 使用规则

- 每完成一个可独立理解、可编译或可测试的增量，就提交一次 commit。
- 本文档只记录：
  - checklist 项
  - 当前状态
  - 对应 commit id
  - 已跑测试
- 尚未提交的工作统一记为 `WORKTREE`。

### 2. 进度表

| 项目 | 状态 | commit | 测试 | 备注 |
| --- | --- | --- | --- | --- |
| `v1` 文档版本信息修正 | 已完成 | WORKTREE | 未跑 | `stdexec` snapshot 改成当前 vendored 版本 |
| `v2` 顶层与模块文档拆分 | 已完成 | WORKTREE | 未跑 | 已新增 `v2.md` 和 5 份模块文档 |
| `SearchTreeCore` 第一阶段抽取 | 进行中 | WORKTREE | 未跑 | 目标是先把 `runSinglePlayout / playoutDescend / maybeCatchUpEdgeVisits` 从 `Search` 主体里抽出 |
| `NNRequestLayer` sender 化第一阶段 | 未开始 | - | - | 先定义值类型与桥接壳层 |
| `TensorRTRuntime` 从 `NNEvaluator` 中内聚抽出 | 未开始 | - | - | 先保持单控制线程 |
| `PlayoutRuntime` 协程化第一阶段 | 未开始 | - | - | 先包装 `async_scope + static_thread_pool` |
| `DemandController` 固定策略版 | 未开始 | - | - | 先打通 permit 闭环 |

### 3. 当前工作树目标

- 补齐第一批实现骨架
- 保持构建可通过
- 为后续 sender / coroutine 迁移留下独立的代码落点

